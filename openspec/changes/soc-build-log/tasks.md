# Tasks

## 1. Печать из шага

- [ ] 1.1 В fhdlgen после разбора топа напечатать путь каждого элемента `frag/include-verilog` этого фрагмента, по одному на строку. Удалить из `fsoc/soc.4th` строки, где несколько имён `*.v` записаны вместе. Проверить, что лог `soc_blink` содержит `stack2.v`, `j1.v`, `uart.v`, `timer.v`, `regio.v`, `j1_wrap.v` и не содержит `tb_timer.v`. `grep` по `fsoc/soc.4th` не находит строку с двумя `.v`.
- [ ] 1.2 В `soc-cross` печатать путь, уже подставленный в команду `gforth`. В `soc-feed-file` печатать путь, уже подставленный в `FSOC_EMU_FEED`. Удалить отдельные `." firmware:` и `." cross:` перед этими вызовами. Проверить по коду, что подпись и аргумент команды — одна строка.

## 2. Проверка

- [x] 2.1 Правило записано в `AGENTS.md`: неизвестный источник не заменяется литералом, вместо этого вопрос пользователю. Не дублировать его вторым файлом правил.
- [ ] 2.2 Прогнать `fmix test`. В логе `projects/soc_blink` секция Hardware содержит по одной строке на каждый скопированный `.v` и `fhdlgen: top.v` из пути фрагмента, без литерала этой строки в `fsoc/soc.4th`, секция Software — пути ядра и подачи, затем `firmware.hex:` с числом байт из 8192. Обновить CHANGELOG.md.
