# Tasks

## 1. Вид консоли

- [x] 1.1 В `emu/con.cc` и `emu/con.h` выбрать вид байта UART. `FSOC_EMU_CON=log` и любое другое непустое значение печатают `t=<ns> uart <имя> <байт>`. `FSOC_EMU_CON=term` пишет сам байт без метки времени. Пустая или отсутствующая переменная: tty на stdout даёт `term`, иначе `log`. Консоль сообщает выбранный `term` вызывающему. Проверить, что `con_pin` по-прежнему печатает только смену уровня и что `main` задачи в `emu/` не появился.

## 2. Сеанс

- [x] 2.1 В `fsoc/soc_main.cpp` при виде `term` и tty на stdin снять флаг `ECHO`, оставить канонический ввод и вернуть атрибуты при выходе, в том числе после SIGINT. Если stdin не tty, `termios` не вызывать. Проверить, что кадр UART и разбор ` ok` не изменились: скрипт с `FSOC_EMU_CON=term` и stdin не tty завершается с тем же кодом 0, что и журнал.
- [x] 2.2 В `fsoc/soc.4th` после `gforth` кросс-компилятора напечатать перевод строки. Проверить, что сообщение `tdp` не склеивается с первой строкой сеанса. `cross.fs` не менять.

## 3. Проверка и документация

- [x] 3.1 В `tests/soc_test.4th` добавить прогон с `FSOC_EMU_CON=term`, `FSOC_EMU_FAST=1` и строкой `1 2 + .`: в файле есть `3` и ` ok`, нет `uart tx`. Прогон с `FSOC_EMU_CON=log` и `FSOC_EMU_UART_BYTES=2` по-прежнему содержит `uart tx 0x0d` и `uart tx 0x0a`. Прогон без переменной в файл тоже остаётся журналом. Прогнать этот тест через `fmix test`.
- [x] 3.2 Обновить README, AGENTS.md и CHANGELOG.md: живой `fsoc --build` на tty показывает текст сеанса, `FSOC_EMU_CON=log` возвращает журнал байтов, `FSOC_EMU_CON=term` принудительно пишет текст. Проверить, что README больше не описывает UART SoC только как строку на каждый байт.
- [x] 3.3 Прогнать `fmix test` и убедиться, что все тесты зелёные, включая `tests/soc_test.4th` и `tests/blinky_test.4th`.
