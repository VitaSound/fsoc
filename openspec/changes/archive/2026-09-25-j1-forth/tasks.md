# Tasks

## 1. Исходники и память

- [x] 1.1 Положить в репозиторий исходники SwapForth J1a и их BSD-текст: `cross.fs`, `basewords.fs`, `nuc.fs`, `swapforth.fs`, `common/core.fs`, `common/core-ext.fs`. Проверить, что файлы на месте, в `nuc.fs` есть `header quit`, а сборка не обращается в сеть за этими файлами.
- [x] 1.2 В `cpu/j1/j1_wrap.v` сделать память как у `j1a/verilator/j1a.v`: 4096 слов, запись в `ram[mem_addr[12:1]]`, выборка команды из `ram[code_addr[11:0]]`. Принять `uart_rx` в `buart`. На чтении `h# 2000` бит 0 — передатчик свободен, бит 1 — `uart_valid`. Проверить, что в обёртке нет `.rx(1'b1)` и нет инстанса `j1_prompt`.

## 2. Образ

- [x] 2.1 Собрать ядро Gforth-ом через `cross.fs` в hex на 4096 слов. Проверить, что строк 4096 и что это выход кросс-компилятора, а не вручную записанный hex.
- [x] 2.2 Вторым шагом прогнать ядро в эмуляции, скормить ему `swapforth.fs`, раскрывая `include` на хосте, и снять RAM в `firmware.hex`. Этот шаг вызывает `soc-emit-emulation`. Проверить, что полный `firmware.hex` отличается от hex одного ядра и тоже содержит 4096 слов.

## 3. Порт приёма и кадр

- [x] 3.1 В `designs/soc_top.4th` добавить вход `uart_rx` и соединить его с обёрткой. Путь выхода оставить только `FSOC_SOC_TOP`. Проверить, что в файле есть `uart_rx`, нет `projects/` и нет `soc_emul`.
- [x] 3.2 В `fsoc/soc_main.cpp` вести кадр 8N1 по 104 такта на бит. После стартовых `0x0d` и `0x0a` послать один CR, затем строку. Строка берётся из `FSOC_EMU_UART_IN` (LF делит строки) либо из stdin до Ctrl+C. Со скриптованным вводом выходить с кодом 0 после ответа ` ok`, CR и LF. Без строки и с `FSOC_EMU_UART_BYTES` выходить после заданного числа байт передачи. `FSOC_EMU_FAST=1` не ждёт стенные часы. Проверить, что `main` лежит в `fsoc/`, а в `emu/` нет файла с именем soc.

## 4. Проверка и документация

- [x] 4.1 Обновить `tests/soc_test.4th` и прогнать его через `fmix test`. С `FSOC_EMU_FAST=1` и `FSOC_EMU_UART_BYTES=2` без строки ввода в журнале ровно `uart tx 0x0d` и `uart tx 0x0a`. Со строкой `1 2 + .` код возврата 0, первый символ не потерян, есть отражение строки, байт `3` и конец ответа: пробел, `o`, `k`, `0x0d`, `0x0a`. Строки `: DOUBLE DUP + ;` и `21 DOUBLE .` дают `4`, `2` и ` ok`. Строки `: P 0 if 1 else 2 then . ;` и `P` дают `2` и ` ok`. Строки `NOWORD` и `1 .` дают `?`, затем `1` и ` ok`. Строка `words` печатает каждое имя из требования словаря. `firmware.hex` совпадает с выходом сборки, в `top.v` есть вход `uart_rx` и нет `j1_prompt`, `fsoc --load` в `projects/soc_emul` даёт код 0.
- [x] 4.2 Обновить README, AGENTS.md, doc/roadmap.md и CHANGELOG.md: `cd projects/soc_emul && fsoc --build` запускает SwapForth J1a, словарь — её ANS CORE без `environment?`, ответ заканчивается на ` ok`. Проверить, что README больше не описывает этот запуск как одноразовые байты `o` и `k`.
- [x] 4.3 Прогнать `fmix test` и убедиться, что все тесты зелёные, включая `tests/soc_test.4th` и прежний `tests/j1_prompt_test.4th`.
