# Tasks

## 1. Листы

- [x] 1.1 Добавить `rtl/timer.v`: порты `clk`, `rst`, `wr`, `din[15:0]`, `value[15:0]`; по `wr` период и счётчик принимают `din`, иначе счётчик убывает и с нуля берёт период, период 0 держит нуль. Проверить, что в файле нет бита адреса и нет `projects/`.
- [x] 1.2 Добавить `rtl/tb_timer.v`, который инстанциирует `timer`. Прогнать Icarus и проверить убывание, повтор после нуля и нуль при периоде 0.
- [x] 1.3 Добавить `rtl/regio.v` с параметром ширины. Инстанс ширины 1 хранит бит: запись сохраняет его, чтение возвращает. Проверить, что модуль не называет плату.
- [x] 1.4 Добавить `rtl/tb_regio.v`. Прогнать Icarus и проверить, что после записи 0 выход 0, после записи 1 выход 1.

## 2. Шина и топ

- [x] 2.1 В `cpu/j1/j1_wrap.v` посадить таймер на `mem_addr[11]` (`h# 800`) и регистр ширины 1 на `mem_addr[10]` (`h# 400`). `wr` — `io_wr` и этот бит, данные — `dout`, чтение попадает в `io_din`. Выход бита вывести из обёртки. Проверить, что биты 12 и 13 по-прежнему UART и что текста модулей таймера и регистра в обёртке нет.
- [x] 2.2 В `designs/soc_top.4th` добавить выход `led` и соединить его с битом обёртки. Путь выхода оставить `FSOC_SOC_TOP`. Проверить, что в файле есть `led`, нет `projects/` и нет имени каталога проекта, а в собранном `top.v` есть выход `led`.

## 3. Копия и 'BOOT

- [x] 3.1 Design-слово хранит пути листов и стендов. Сборка копирует `timer.v`, `tb_timer.v`, `regio.v`, `tb_regio.v` в переданный каталог. Проверить, что в `fsoc/soc.4th` нет строки `soc_blink` и нет `setenv`.
- [x] 3.2 Добавить ячейку `'BOOT`. Пустая ячейка не вызывает слово перед `quit`. Прогнать `projects/soc_emul` с `FSOC_EMU_FAST=1` и `FSOC_EMU_UART_IN=1 2 + .` и проверить код 0, байт `3` и ответ ` ok`.

## 4. Проект blink

- [x] 4.1 Добавить `projects/soc_blink/target.4th`: задача SoC, таргет emulation, признак образа blink задаётся до слова сборки. Слово сборки по признаку дописывает цикл и xt в `'BOOT`. Проверить, что `git check-ignore` не прячет этот `target.4th` и что каталог `soc_blink` не назван в `designs/`.
- [x] 4.2 Цикл пишет 0 и 1 в `h# 400`, между записями читает `h# 800` и шлёт в UART текст о включении и выключении лампы. `'BOOT` вызывает цикл и не возвращается в `quit`. `main` blink вызывает `con_pin` для `led` и после двух смен выходит с кодом 0 при `FSOC_EMU_FAST=1`. `main` `soc_emul` `con_pin` не вызывает.
- [x] 4.3 В `tests/` добавить прогон через `fmix test`. С `FSOC_EMU_CON=term` и `FSOC_EMU_FAST=1` в выводе есть обе фразы лампы и нет ` ok`. В журнале пинов есть `pin led 1` и `pin led 0`. Копии `timer.v` и `regio.v` в каталоге проекта совпадают с `rtl/`.

## 5. Документация

- [x] 5.1 Обновить README, AGENTS.md и CHANGELOG.md: проект blink мигает `led` из Форта, пауза — `h# 800`, текст лампы в `term`, уровни в журнале пинов. Фон и контроллер прерываний остаются будущим шагом из `doc/stm8ef-hw.md`. Прогнать `fmix test` и убедиться, что зелёные и `tests/soc_test.4th`, и новый прогон blink.
