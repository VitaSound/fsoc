build/
# Project dirs are working copies. Git keeps only the manifest; --build
# writes top.v, firmware.hex, sim.sh, .qsf, obj_dir, … locally.
projects/*
!projects/*/
projects/*/*
!projects/*/target.4th
forth-packages/
.fcov/
.fmcp/
*.o
out.vcd
trace.vcd
/tmp/
firmware/firmware.hex
